Excercise 1.3
=============

(define sum-squares (lamba (x y)
  (+ (*x x) (* y y))))



(define square-largest (lambda (x y z)
  (cond ((and (z <= x) (z <=y) (sum-squares x y))
	((and (y <= x) (y <=z) (sum-squares 

))